import 'package:flutter/material.dart';

class NoInternetHandlingPage extends StatelessWidget {
  const NoInternetHandlingPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('No Internet')));
}
