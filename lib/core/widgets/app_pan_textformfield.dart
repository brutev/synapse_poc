import 'package:flutter/material.dart';

class AppPanTextFormField extends StatelessWidget {
  const AppPanTextFormField({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
