import 'package:flutter/material.dart';

class AppMobileTextFormField extends StatelessWidget {
  const AppMobileTextFormField({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
