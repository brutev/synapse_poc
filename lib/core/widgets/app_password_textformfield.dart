import 'package:flutter/material.dart';

class AppPasswordTextFormField extends StatelessWidget {
  const AppPasswordTextFormField({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
