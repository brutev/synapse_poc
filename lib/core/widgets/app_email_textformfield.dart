import 'package:flutter/material.dart';

class AppEmailTextFormField extends StatelessWidget {
  const AppEmailTextFormField({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
