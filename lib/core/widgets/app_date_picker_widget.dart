import 'package:flutter/material.dart';

class AppDatePickerWidget extends StatelessWidget {
  const AppDatePickerWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
