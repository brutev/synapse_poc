import 'package:flutter/material.dart';

class DynamicStepperWidget extends StatelessWidget {
  const DynamicStepperWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
